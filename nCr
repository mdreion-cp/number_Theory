ll nCr(ll n, ll r) {
    if (r < 0 || r > n) return 0;
    if (r > n - r) r = n - r; // symmetry

    ll ans = 1;
    for (ll i = 1; i <= r; i++) {
        ans = ans * (n - i + 1) / i;
    }
    return ans;
}
